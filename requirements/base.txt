# Shared dependencies for the data loaders (src/loading) and the benchmark
# infrastructure (feature_extraction_benchmark.py, src/base.py).
#
# Lower bounds are chosen to be compatible with BOTH Python 3.7 (kats) and
# Python 3.12 (all other environments): uv/pip resolves to the newest version
# that is compatible with the interpreter used in each .venv.
#
# To produce fully pinned, reproducible lock files run:
#   scripts/lock_requirements.ps1   (Windows)
#   scripts/lock_requirements.sh    (Linux/macOS)

numpy>=1.21
pandas>=1.3
scipy>=1.7
scikit-learn>=1.0
statsmodels>=0.12
psutil>=5.9
tqdm>=4.66
toml>=0.10
matplotlib>=3.5
h5py>=3.7
# Test runner: the README documents `pytest` for the test suite, so it must be
# available in every venv. pytest >= 8 requires Python >= 3.8; on the kats
# (Python 3.7) venv the resolver falls back to the last 3.7-compatible release.
pytest>=7.0
# Data-loader runtime dependencies (NOT optional):
#  - fsspec: used by src/loading/cnc_mill_tool_wear.py to fetch the dataset from
#    GitHub (fsspec.filesystem("github", ...)). The github filesystem backend is
#    bundled in the fsspec package itself.
#  - gitpython: used by src/loading/bosch_cnc_machining.py to clone the Bosch
#    dataset repository.
# Lower bounds compatible with both Python 3.7 (kats) and 3.12 (all others).
fsspec>=2023.1.0
gitpython>=3.1.24
# requests is required by the fsspec "github" filesystem backend used by
# src/loading/cnc_mill_tool_wear.py (fsspec lazily imports requests).
requests>=2.28
# setuptools provides `pkg_resources`, which some transitive deps require at runtime
# (e.g. TPOT -> stopit -> pkg_resources). uv venvs do not include it by default.
# NOTE: setuptools >= 81 REMOVED pkg_resources, so pin below 81.
setuptools>=70,<81
